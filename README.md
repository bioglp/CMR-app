# CMR-app

A mobile-first progressive web app for field capture-mark-recapture data collection. Records new captures and recaptures, exports CSV/JSON, and estimates survival (φ) and recapture probability (p) via Cormack-Jolly-Seber MLE. Runs entirely in the browser with no server or dependencies required.
